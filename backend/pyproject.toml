[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# E/W: pycodestyle  F: pyflakes  I: import sorting
select = ["E", "W", "F", "I"]
ignore = [
    "E501",  # long lines are common in test fixtures and URL strings
]
